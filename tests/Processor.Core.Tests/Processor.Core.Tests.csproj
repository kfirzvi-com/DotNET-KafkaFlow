<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Processor.Core.Tests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.2" />
    <PackageReference Include="Microsoft.Extensions.TimeProvider.Testing" Version="9.10.0" />
  </ItemGroup>

  <ItemGroup>
    <!-- Core only: the shared logic is tested against a synthetic domain (see Support/TestDomain.cs),
         so these tests cannot accidentally depend on a real domain's rules. -->
    <ProjectReference Include="..\..\src\Processor.Core\Processor.Core.csproj" />
  </ItemGroup>

</Project>
